import { PortalLayout } from '@/components/layout/PortalLayout'

export default function PanchakarmaSessionsLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return <PortalLayout>{children}</PortalLayout>
}
